<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SplitEase - Expense Tracker</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Background Image */
    body {
      background: url('https://source.unsplash.com/1600x900/?finance,budget') no-repeat center center/cover;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    /* Dark overlay for readability */
    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.6);
      z-index: 1;
    }

    /* Glassmorphism container */
    .container {
      position: relative;
      z-index: 2;
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(12px);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      padding: 2.5rem;
      border-radius: 15px;
      text-align: center;
      width: 90%;
      max-width: 450px;
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    /* Heading */
    .title {
      font-size: 2.5rem;
      font-weight: 700;
      color: white;
      margin-bottom: 0.5rem;
    }

    .subtitle {
      font-size: 1rem;
      color: #e0f2fe;
      margin-bottom: 2rem;
    }

    /* Buttons */
    .button {
      display: block;
      width: 100%;
      padding: 0.75rem;
      margin-top: 1rem;
      border-radius: 8px;
      font-size: 1.2rem;
      font-weight: 600;
      text-align: center;
      transition: all 0.3s ease-in-out;
    }

    .register {
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white;
    }

    .register:hover {
      transform: scale(1.05);
      background: linear-gradient(135deg, #2563eb, #1e40af);
    }

    .login {
      background: linear-gradient(135deg, #22c55e, #16a34a);
      color: white;
    }

    .login:hover {
      transform: scale(1.05);
      background: linear-gradient(135deg, #16a34a, #15803d);
    }
  </style>
</head>
<body>

  <div class="container">
    <h1 class="title">SplitEase</h1>
    <p class="subtitle">Track and settle expenses with ease.</p>

    <a href="/register" class="button register">Register</a>
    <a href="/login" class="button login">Login</a>
  </div>

</body>
</html>
