<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Expense</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background: url('https://source.unsplash.com/1600x900/?finance,money') no-repeat center center/cover;
      min-height: 100vh;
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen bg-black/50">

  <div class="bg-white/10 backdrop-blur-lg shadow-2xl p-8 rounded-lg w-full max-w-md border border-white/20 text-white">
    
    <h1 class="text-3xl font-extrabold text-center mb-6 tracking-wide">Add Expense</h1>

    <form action="/add-expense/<%= roomId %>" method="POST" class="space-y-5">
      
      <div>
        <label for="description" class="block text-lg font-medium">Description</label>
        <input type="text" name="description" id="description" 
               class="w-full px-4 py-2 rounded-lg bg-white/10 text-white border border-white/30 shadow-inner focus:outline-none focus:ring-2 focus:ring-green-500" required>
      </div>

      <div>
        <label for="amount" class="block text-lg font-medium">Amount</label>
        <input type="number" name="amount" id="amount"
               class="w-full px-4 py-2 rounded-lg bg-white/10 text-white border border-white/30 shadow-inner focus:outline-none focus:ring-2 focus:ring-green-500" required>
      </div>

      <button type="submit" 
              class="w-full bg-gradient-to-r from-green-500 to-green-600 text-white py-3 rounded-lg shadow-md hover:scale-105 transition-transform duration-300 font-bold">
        Add Expense
      </button>
    </form>

    <p class="mt-5 text-center text-white/80">
      <a href="/room/<%= roomId %>" class="text-green-400 underline hover:text-green-300 transition-colors">Back to Room</a>
    </p>
  </div>

</body>
</html>
